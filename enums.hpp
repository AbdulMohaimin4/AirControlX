#ifndef ENUMS_HPP
#define ENUMS_HPP

enum class FlightType { Commercial, Cargo, Military, Medical };

enum class AirlineName { PIA, AirBlue, FedEx, Pakistan_Airforce, Blue_Dart, AghaKhan_Air_Ambulance }; 

enum class AircraftPhase { Holding, Approach, Landing, Taxi, AtGate, Takeoff, Climb, Cruise };

enum class Direction { North, South, East, West, None };

enum class RunwayID { RWY_A, RWY_B, RWY_C };

#endif
